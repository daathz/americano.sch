<% include ./partials/header %>
<form action="POST">
    <div class="row">
        <div class="col-md-5 offset-md-1">
            <h2>Order</h2>
            <form method="POST">
                <% foods.forEach(function(food) { %>
                    <div class="card" style="margin-bottom: 20px;">
                        <div class="card-body">
                            <h5 class="card-title"><%= food.name %></h5>
                            <p class="card-text"><%= food.description %></p>
                            <div class="form-inline">
                                <label for="hamburger" style="margin-right: 10px;">Quantity</label>
                                <input id="hamburger" class="form-control" type="number" name="<%= food.seo_name %>" placeholder="0">
                            </div>
                        </div>
                    </div>
                <% }) %>
            </form>
        </div>
        <div class="col-md-5">
            <h3>Comments</h3>
            <textarea class="form-control" name="comment" rows="5"></textarea>
            <button type="submit" class="btn btn-primary" style="margin-top: 10px;">Order</button>
        </div>
    </div>
</form>
<% include ./partials/footer %>